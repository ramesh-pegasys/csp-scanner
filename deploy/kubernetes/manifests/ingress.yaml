apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: cloud-artifact-extractor
  namespace: cloud-artifact-extractor
  labels:
    app: cloud-artifact-extractor
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/rate-limit: "100"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  tls:
  - hosts:
    - api.example.com
    secretName: cloud-artifact-extractor-tls
  rules:
  - host: api.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: cloud-artifact-extractor
            port:
              number: 80

---
# If using cert-manager for automatic SSL
# First: kubectl apply -f https://github.com/cert-manager/cert-manager/releases/download/v1.13.0/cert-manager.yaml

# Uncomment below to auto-issue certificate
# apiVersion: cert-manager.io/v1
# kind: Certificate
# metadata:
#   name: cloud-artifact-extractor-cert
#   namespace: cloud-artifact-extractor
# spec:
#   secretName: cloud-artifact-extractor-tls
#   duration: 2160h # 90d
#   renewBefore: 720h # 30d
#   commonName: api.example.com
#   dnsNames:
#   - api.example.com
#   issuerRef:
#     name: letsencrypt-prod
#     kind: ClusterIssuer
